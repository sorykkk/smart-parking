# FindSpot Environment Configuration
# Optimized for Raspberry Pi 5 deployment
# Copy this file to .env and configure for your setup

# Security (REQUIRED - these MUST be set!)
SECRET_KEY=your-secret-key-here
MQTT_PASSWORD=backend_password

# MQTT Configuration
# ESP32 device configuration (prefix, salt) is automatically detected from ESP32 during registration

# Backend MQTT user
MQTT_USER=flask_backend

# Note: ESP32 devices use usernames from their Config.h (e.g., 'esp32_dev_<MAC>')
# Backend uses 'flask_backend' username
# Both types of users are automatically managed

# Raspberry Pi 5 Network Configuration
# Set this to your Pi's IP address for ESP32 devices to connect
# Update hw/src/Config.h MQTT_BROKER to match this IP
PI_IP_ADDRESS=192.168.1.100

# Optional: Change default ports if needed
# BACKEND_PORT=5000
# FRONTEND_PORT=80
# MQTT_PORT=1883

# Raspberry Pi 5 Performance Optimizations
# Uncomment these for better performance on ARM architecture
# DOCKER_BUILDKIT=1
# COMPOSE_DOCKER_CLI_BUILD=1
