# Smart Parking System - Centralized IP Configuration
# ========================================================
# Edit this file to change the IP address used across the entire sw/ stack
# After changing IP_ADDRESS, run: .\set-ip.ps1 to apply changes everywhere

# Main IP Address - Change this to your server/Raspberry Pi IP address
IP_ADDRESS=192.168.1.102

# Derived URLs (these are computed automatically)
# Backend URL: http://${IP_ADDRESS}:5000
# Frontend URL: http://localhost:5173 (dev) or http://${IP_ADDRESS}:80 (production)
# MQTT Broker: ${IP_ADDRESS}:1883
